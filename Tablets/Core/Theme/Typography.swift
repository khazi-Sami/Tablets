import SwiftUI

enum Typography {
    static let largeTitle = AppFont.display
    static let title = AppFont.title
    static let headline = AppFont.sectionTitle
    static let body = AppFont.body
    static let bodyMedium = AppFont.bodyStrong
    static let caption = AppFont.caption
    static let button = AppFont.button
}
